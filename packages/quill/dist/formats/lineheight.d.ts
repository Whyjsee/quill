import { ClassAttributor } from 'parchment';
declare class LineHeightClass extends ClassAttributor {
    add(node: HTMLElement, value: any): boolean;
}
declare const LineHeight: LineHeightClass;
export default LineHeight;
